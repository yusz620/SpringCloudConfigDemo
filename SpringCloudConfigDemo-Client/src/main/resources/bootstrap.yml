spring:
  application:
    name: foo
  profiles:
    active: production
  cloud:
    config:
      uri: http://localhost:8888
      label: master
  jta:
    bitronix:
      properties:
        debug-zero-resource-transaction: true
